name: CI

on:
  push:
    branches: [main, master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  # Always-run: build + type-check + surface-level tests + tarball validation.
  # No network calls to YouTube — works on any runner.
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: [20, 22]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: 'npm'

      - run: npm ci

      - run: npm run build

      # Tests are split: surface-level tests run here; live-network tests
      # are gated by YOUTUBE_LIVE=1 (see live-check.yml).
      - run: npm test

      - name: Test production canary retries
        run: node --test .github/scripts/check-production-api.test.mjs

      - name: Test caption API retries and caching
        if: matrix.node == 22
        run: |
          npm ci --prefix sample/server
          npm test --prefix sample/server

      - name: Verify published tarball contents
        run: |
          npm pack --dry-run 2>&1 | tee pack-output.txt
          grep -q "dist/index.js" pack-output.txt
          grep -q "dist/index.d.ts" pack-output.txt
